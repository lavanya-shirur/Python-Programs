import random
def shuffle(arr):
    for i in range(len(arr)):
        j=random.randint(i,len(arr)-1)
        print(i,j)
        temp=arr[i]
        arr[i]=arr[j]
        arr[j]=temp
    return arr
arr=['l','a','v','a','n','y','a']
print(shuffle(arr))
